# Real Futures Production provenance incident — read-only audit, 2026-09-24 UTC

## Decision and scope

**RELEASE BLOCKED.** The exact technical path that changed the live Production runtime is established. In this conversation on 2026-09-24, the owner explicitly stated that they did **not** approve publication of the exact deployed SHA. Authorization classification: **UNAUTHORIZED**. The earlier 24-hour Shadow remains permanently **FAIL** (186 stale inputs); the separate short candle-boundary correction test remains **PASS**; no fresh 24-hour Shadow has started. Native Binance/MEXC protection and fresh Real accounting remain **NATURALLY PENDING**. No trade, exchange write, Production DB write, rollback, deployment, or application/configuration change was performed by this audit.

Evidence was collected read-only on 2026-09-24 UTC. The relevant observed transition was approximately 2026-09-23T20:58:18Z (live service start/symlink swap); the deployed marker was written at 20:58:20.292879Z. The approved frozen runtime was `b88f25ac760b5af7cd6d3b626530fb1b3e9e5397`; marker, active symlink and live process cwd independently pointed to `85aedfb944a67c94227ac343e911bc64a581e79e`. The main service was active with Node at `/opt/signalverse/app/.runtime/server.mjs`, started 20:58:18Z, `Result=success`, `NRestarts=0`. The previous-release symlink still pointed to `b88f25ac760b5af7cd6d3b626530fb1b3e9e5397`.

A final read-only check at `2026-09-24T11:42:40Z` again found marker, active symlink and process cwd at `85aedfb944a67c94227ac343e911bc64a581e79e`, with main PID `1962862`, service active, `Result=success`, `NRestarts=0`. This audit caused no Production change.

## Git lineage and content classification

`85aedfb` is a linear descendant of `b88f25a` on `main`/`origin/main`/`origin/HEAD`, not a merge or rebase-only alias. No tag contains it and no associated pull request was returned. The intervening commits were:

| SHA | Commit time UTC | Subject / role |
| --- | --- | --- |
| `8cc826bb848a93ff673a5b349f082e6331a8cd80` | 2026-09-22 17:29:49 | GitHub logging gate documentation, `[skip ci]` |
| `479407d7ae9cfaf2b2d13fd7cfe2768ed9a432e9` | 2026-09-22 17:49:30 | Futures natural-gates documentation, `[skip ci]` |
| `d0454c5e58f00fdb924f5f95396e26feb8723a43` | 2026-09-23 08:25:10 | Failed Shadow documentation, `[skip ci]` |
| `ee16a184d35e9cd485aa06f2cbba908901b357b4` | 2026-09-23 16:20:51 | Stablecoin Reserve Bank, described as Demo-only; executable/UI/migration changes |
| `85aedfb944a67c94227ac343e911bc64a581e79e` | 2026-09-23 20:54:27 | Stablecoin test correction; executable feature from its parent was packaged in the successful run |

Git author/committer identity for the latter commits is Farzam Feali; their commit messages credit Claude Sonnet as coauthor. A Git identity or coauthor line is not proof of approval to deploy. The exact seven-file diff from `b88f25a` to `85aedfb` is:

| Category | File | Behavior and scope |
| --- | --- | --- |
| A — trading/execution logic | `api/stablecoin-engine.ts` | Adds Demo Reserve Bank refill/re-evaluation to allocation cycles and admin/viewer reserve actions. Successful allocation still uses the existing Demo simulation path that writes Demo DB inventory/trades. The existing `real-execute` endpoint remains HTTP 501. No Real Futures or Spot Copytrade execution path was changed. |
| D — database/migration | `migrations/stablecoin_reserve_bank.sql` | Adds two Stablecoin Demo reserve tables and three reserve RPCs with service-role grants. It does not define Futures/Spot tables or protection/accounting changes. |
| G — UI | `src/app/App.tsx` | Adds Reserve Bank controls and status within the Stablecoin Demo panel. No Futures, Spot Copytrade or Fast Trader UI diff. |
| H — documentation/tests only | `HANDOFF.md`; `docs/testing/github-event-log-403-remediation-2026-09-22.md`; `docs/testing/real-futures-natural-gates-checkpoint-2026-09-22.md`; `scripts/stablecoin-engine-test.mjs` | Handoff, audit records and Stablecoin test expectations; no runtime behavior from these files. |

Categories B (Decision Engine/Strategy/Risk), C (exchange integration), E (deployment/runtime/configuration), F (telemetry/logging) and I (other) have no changed file. `api/copytrade.ts`, `api/analyze.ts`, exchange adapters, workflow, dependency manifests and Futures migrations are unchanged. The copytrade and analyze runtime bundles have identical hashes across the two releases after normalizing their release-specific build-path banner. The changed Stablecoin code can affect Demo DB state and admin UI; the diff shows no new exchange-order call, credential handling, connectivity, Futures protection, reconciliation, accounting or scheduling change. This is a code-scope finding, not a guarantee about all live exchange state.

## CI/CD and VPS activation chain

The `Production CI` workflow is configured to run on pushes to `main`/`master`; its package and delivery steps run for push events. It has no job-bound GitHub Environment approval and no exact-SHA release allowlist. The GitHub `Production` environment returned `protection_rules=[]`; private-repository branch-protection/ruleset API checks returned HTTP 403 due account-plan access, so branch rules could not be independently established.

- The parent feature commit `ee16a184...` triggered [run 35888129325](https://github.com/signal0verse/signalverse-main/actions/runs/35888129325) at 2026-09-23T16:21:05Z. It failed the Stablecoin engine test at 16:22:28Z and did not reach delivery.
- The test-fix commit `85aedfb...` triggered [run 35919072919](https://github.com/signal0verse/signalverse-main/actions/runs/35919072919) by a **push** to `main`, actor account `signal0verse`, not `workflow_dispatch` or a schedule. The run began at 2026-09-23T20:54:38Z and succeeded at 20:58:26Z. Its job `Build web and API runtime` (107378066113) ran 20:54:42Z–20:58:25Z. Package and `Deliver release to production` succeeded; delivery ran 20:56:58Z–20:58:24Z. The log recorded receiver `accepted=true` for the exact SHA at 20:56:59.364Z and `status=deployed` at 20:58:24.332Z. The three documentation commits had no Actions delivery run.
- The workflow obtains a GitHub OIDC token for audience `signalverse-production`, POSTs the packaged archive to `/_deploy/<SHA>`, then polls deployment status. The VPS receiver checks OIDC issuer/audience, repository, push event, main/master ref, workflow ref and SHA/path consistency. It does **not** check an out-of-band owner-approved SHA.
- The receiver started `signalverse-deploy-artifact@85aedfb944a67c94227ac343e911bc64a581e79e.service`, whose `ExecStart` was `/usr/local/sbin/signalverse-deploy 85aedfb944a67c94227ac343e911bc64a581e79e`. Its journal shows one start at 20:56:59Z, one successful completion/deactivation at 20:58:20Z, and no evidence of a second activation. The deploy script built the release, swapped app/admin symlinks, restarted the service and observer, passed health checks, and wrote the deployed marker. The main service journal shows one stop/start at 20:58:18Z. A transient localhost health connection refusal during restart recovered. The deploy receiver's own journal had no transition-specific message; the CI accepted response and artifact-unit journal are the positive evidence.
- Incoming archive mtime was 20:56:59Z; release directory mtime approximately 20:58:17Z; active symlink mtime 20:58:18.681895Z; marker mtime 20:58:20.292879Z. The old `b88f25a` release remained available as previous-release. The deployment was an automatic consequence of a successful executable-bearing `main` push, but the actor who approved that push as a Production release has not been established.

The receiver and deploy-script files predated this transition; Production env file, service unit and Futures cron-gate mtimes likewise showed no transition-time change. The Futures Pro cron gate was already enabled before this deployment. A successful service health check is not proof of correct Real trading behavior.

## Read-only database and Real-account impact

The active database was identified from live PostgREST connections, and queries used a read-only transaction. The new reserve tables and three RPCs were present. The earliest reserve event was `RESERVE_SET` at 2026-09-23T20:37:38.778353Z, proving that the schema existed **before** the 20:58 deployment. Neither the deployment script nor its journal shows a migration step; the precise DDL execution time and author remain unknown. The migration file's “Applied” comment and earlier commit/handoff “not yet applied” note conflict; the DB observation controls the fact of application, not its provenance.

For the approximately 20:54–21:05Z transition interval, DB queries found **0** newly created/updated Futures execution attempts, **0** Real trade opens/closes, **0** Real pending creates/triggers, **0** Real-account row updates, and **0** Stablecoin Demo trades. Two `MANUAL_ADD` reserve events occurred after activation (21:02:52–21:03:25Z), and two Demo inventory rows had 20:58:31Z update timestamps. These facts do not prove that deployment itself initiated a trade or those DB writes.

Between activation and the later 2026-09-24 audit, DB records did show **4** new Binance Real Futures execution attempts, all `COMMITTED`, and **4** corresponding Real trade opens: **2** still marked `OPEN` and **2** later closed `SL_HIT`. The first attempt was at 2026-09-23T21:20:11Z; the last at 2026-09-24T05:30:21Z. Four stop-loss and four take-profit placement timestamps were recorded, plus two funding reconciliation timestamps. Two open trade rows were marked `SAFE` with SL/TP order IDs, but these are database assertions **not** independent live-exchange protection verification. Four legacy MEXC `OPEN` DB rows had no protection IDs; their live exchange state was not checked. Four Real pending rows later triggered and one later failed. There were **169** Stablecoin Demo trades and **4** post-activation reserve events (`MANUAL_ADD`, no `AUTO_REFILL`). The exact causes of these later events cannot be attributed to the deploy alone; preexisting schedules and account activity remain possible.

No private exchange API was called. Actual exchange orders, balances, leverage, credentials and exchange settings therefore cannot be certified unchanged or reconciled from this audit. No `real_accounts` row update was observed since activation and config-file mtimes were stable; those narrower facts do not establish private-account invariants. The four `COMMITTED` attempts are a material Real activity signal requiring a later authorized exchange reconciliation, not a reason to manufacture a trade or claim a natural gate passed.

## Authorization, preventive control and remaining gates

The available handoffs, Git history, associated PR query (none), GitHub environment and workflow logs contain no explicit owner approval for Production release of exact `85aedfb`. More decisively, the owner stated in this conversation on 2026-09-24 that they did not approve that release. The single authorization/root-cause classification is therefore **UNAUTHORIZED**, supported by the owner's direct statement rather than inferred merely from absent records. The *mechanism* is proven (push-triggered CI/OIDC receiver/artifact deploy). The main-push auto-promotion and missing exact-SHA approval guard are confirmed deployment-control weaknesses; the individual who pushed/caused the commit and any other permissions or intent beyond the observed Git identity are not established by this audit.

A preventive deployment-control fix is needed before another release. Proposed minimal design, **not implemented**: require a root-controlled, out-of-band, one-time release authorization binding the exact commit SHA, repository/ref, workflow and expiry; check it fail-closed at the receiver before archive acceptance and again immediately before symlink activation; record a sanitized approval/audit receipt; reject wrong, missing, replayed, docs-only and stale approvals. A separate manually approved deploy job/GitHub Environment reviewer can add defense in depth if plan capabilities permit, but must not be the sole guard. Test the control with denied and approved exact-SHA cases before authorizing a deployment. Do not alter Futures Engine, Strategy, Risk, exchange adapters, accounting or protection for this provenance fix. No preventive fix, rollback or Production deployment occurred during this audit.

The release cannot advance until the owner explicitly selects an approved Production SHA, the unauthorized deployment incident is resolved, required controls are addressed and validated, and only then a **fresh** 24-hour prospective Shadow is separately scheduled. No rollback is implied or attempted. The short boundary PASS does not override the prior 24-hour FAIL or prove profitability/AI Supervisor effectiveness. Natural native-protection and Real-accounting gates remain **NATURALLY PENDING**. Current Production runtime observed in this audit: `85aedfb944a67c94227ac343e911bc64a581e79e`; Production changed due to the earlier deployment, **not** due to this audit.

RELEASE BLOCKED
