# Real Futures — exact c518 Production release and final audit, 2026-09-22 UTC

## Metadata

- Date: 2026-09-22 UTC
- Task ID: owner-authorized exact Real Futures release `c518c3e`
- Module: Real Futures (Binance/MEXC), release infrastructure, PostgreSQL
- Mode: authorized Production backup, migration and deployment; bounded read-only post-deploy verification. **No manufactured Real trade.**
- Application repository: `signal0verse/signalverse-main`
- Previous active Production SHA and candidate parent: `56aaff32ba003850db2954d9199cd27cdeaf954a`
- Exact deployed executable: `c518c3e66a68afc5c7e8e45d8ed32d198364419a`
- Candidate tree: `f8a5b5955dc3692e767ab1185267540503213c45`
- Critical Git blobs: `api/copytrade.ts` `544d4486c2337950b09bac09b781f27c74f65847`; `api/analyze.ts` `67e9f7449ba749d09b827c46ec2a022e9125585c`
- Documentation-only relative-to-c518 integration merge on `main`: `e9aced1c8ac585688947c8c6703ded939b9ea7fe`, parents `c518c3e...` and the existing Spot documentation commit `623831fa78a5e824a35906a35dbda5170ab897b0`. **This is not the deployed runtime.**

## Objective and scope

The owner explicitly authorized completing the exact c518 release end-to-end: fresh backup and restore validation, two specified Futures migrations, schema proof, exact-SHA CI/CD deployment, runtime/regression/safety verification, and live native-protection/accounting checks **only if natural suitable Real lifecycles existed**. No position could be opened, resized, reversed or closed just to satisfy a test; no unrelated Spot/Demo/Shadow/Strategy/Engine settings, credentials or code could be changed.

## Actions and evidence

1. **Freeze/preflight — PASS.** The isolated c518 checkout was clean. Its parent, tree and critical blobs matched the prior integrated audit. A read-only VPS check before all writes showed both `/opt/signalverse/app` and `/var/lib/signalverse-deploy/deployed-sha` at exact `56aaff32...`, with the main service active. Remote `main` was `623831fa...`, whose delta from `56aaff32...` was Spot documentation only. Remote `master` did not exist. The deployed receiver was inspected and explicitly permits OIDC `production-ci.yml` push claims on `main` or `master`, bound to the package SHA.
2. **Full Production backup — PASS.** Active PostgreSQL 17.11 database `signalverse_cutover2` was backed up with `pg_dump -Fc` to `/var/backups/signalverse/pre-c518-20260922T1601Z.dump` at 16:01 UTC. File SHA-256: `a74d3aa058cdede0b27d7b992acc76481ef17d4497a417b2f131b344db4504df`; approximately 68 MiB compressed. Parent directory mode `700`, file mode `600`, owned by `postgres`. `pg_restore --list` succeeded. A complete `pg_restore --exit-on-error` to a newly created disposable VPS database succeeded; 88 public tables, 1203 `copy_trades` and 118 `futures_execution_attempts` were observed there, matching pre-migration Production aggregates. Only this disposable database was dropped afterwards; the backup remains. No private row data or backup file was transferred into this report.
3. **Exact migrations — PASS.** Transferred only `migrations/futures_reanalysis_claims.sql` (Git blob `6499ce0b254b49e345330a914c25afd5825c9555`, SHA-256 `98e78ea48d039896dfc35f8e497edc3c7f27c5951059deee3c65c06c03bd013d`) and `migrations/futures_real_portfolio_capacity.sql` (Git blob `51dc89f4b0010c16dd23edc3f047785f0e40a69b`, SHA-256 `0fe4d12c1b434be4bd9f7899889a44b989a647868eb3471316acb03bd27bec16`). VPS hashes matched the local exact-candidate files before execution. Their redundant outer transaction wrappers were removed **only from the input stream**, with both original SQL bodies executed in the specified order inside one explicit `BEGIN`/`COMMIT` through `psql -X -v ON_ERROR_STOP=1 -d signalverse_cutover2`. The SQL output ended in `COMMIT`, with no failure or rollback. No unrelated migration ran.
4. **Post-migration schema — PASS.** Public table count 88→90; existing row aggregates still 1203/118; new claim/pending rows 0/0. All four reanalysis RPCs and the portfolio-capacity function existed. Claim/pending constraints 3/7 included primary/unique/check/foreign-key requirements; indexes 2/2; one BEFORE INSERT capacity trigger pointed to `guard_futures_portfolio_capacity()`. Both tables had RLS. Service role held the tested table and RPC permissions; `anon` could not select them or execute the four RPCs. Standard PostgREST `NOTIFY pgrst, 'reload schema'` succeeded; service remained active. One **read-only verification query** initially had a harmless string-concatenation type error, corrected and rerun successfully. An earlier **read-only** restored-table-count query had a shell-quoting error, corrected before removing the disposable DB. Neither error was a migration or data mutation.
5. **Exact deployment — PASS.** Published c518, not a branch-tip rebuild, to the previously absent `master` release ref. [Production CI run 35751982028](https://github.com/signal0verse/signalverse-main/actions/runs/35751982028) had `headSha=c518c3e...`, all tests/build/bundle/package/delivery steps successful, and completed at 16:12:22 UTC. The established OIDC receiver and `/usr/local/sbin/signalverse-deploy` activated the release. At 16:12:51 UTC, main/admin symlinks, deploy marker, main process cwd and admin health self-report all identified exact c518. Deployed source Git hashes for `api/copytrade.ts` and `api/analyze.ts` matched the approved blobs. Deploy unit result `success`; no rollback.
6. **Main-line preservation — PASS, not a second release.** `main` had a newer Spot documentation-only child of the previous executable. To prevent a later normal `main` push silently restoring old Futures code, both histories were merged with an explicit conflict resolution that preserved both handoffs. The resulting `e9aced1...` tree differs from c518 only in `HANDOFF.md` and three `docs/testing/` files. Executable code, migrations, workflow and dependency files are identical to c518. It was fast-forward pushed to `main` with `[skip ci]`, per the repository's docs-only handoff practice and [GitHub's push-skip behavior](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/skip-workflow-runs). No CI run was created for that merge. A new VPS check confirmed runtime/marker/admin SHA still c518.
7. **Immediate core runtime — PASS; pre-live-write API-error gate — FAIL.** Main, admin, observer, PostgREST, PostgreSQL and isolated Shadow services active; main/admin health successful; public web, `/healthz` and `/api/public-info` HTTP 200. At the final 16:22:20 UTC check, both runtime symlink and marker plus admin self-report still identified c518. After the 16:12:22 deployment marker, database aggregates showed zero newly opened or closed Real `copy_trades`, zero new `futures_execution_attempts`, and zero reanalysis claims/pending. There was no observed application-recorded Real entry attempt or newly closed Real lifecycle. This does **not** prove absence of every external exchange-side event. Repeated GitHub logging API `403` violates the owner's strict no-repeated-API-errors condition before any operator-initiated live exchange write; none was attempted.
8. **Spot/Demo/Shadow bounded regression — PASS at service/data-freshness level.** Open Spot real/demo cycles and open Demo Futures rows continued normal last-sync updates at 16:15 UTC after the release. The independent `sv-futures-shadow-20260922.service` remained active. These observations do not prove private Spot fill correctness, Demo PnL or the separate 24-hour Shadow protocol; no mode/configuration change was made.

## Live native and accounting gates

| Gate | Status | Exact missing evidence |
| --- | --- | --- |
| Binance Real native SL/TP replacement and read-back | **NATURALLY PENDING** | Zero app `OPEN` Binance Real trades at release check. Need a naturally suitable open position, pre-write risk/identity proof, bounded replacement through existing path, both native legs confirmed, old legs retired only afterwards, and DB reconciliation. No test trade was opened. |
| MEXC Real native plan-order replacement and read-back | **NATURALLY PENDING** | Four old DB `OPEN` MEXC rows were from 2026-08-28; none had a matching currently connected Real account row, native SL/TP IDs or a last-sync timestamp. They cannot be presumed live, eligible or protected. Native position/plan-order truth must be reconciled first, without changing credentials or manufacturing a position. |
| Fresh Real accounting | **NATURALLY PENDING** | Zero naturally closed Real trades since deployment. Need entry/exit fills and quantities/prices, commission asset/direction/value, funding, exchange realized PnL, dedup IDs, immutable app ledger and final DB state for a new lifecycle. |
| Profitability and AI Supervisor benefit | **NOT APPLICABLE / NOT VERIFIED** | This correctness/deployment release generated no prospective profit or independent AI-effect sample. |

**Real order writes in this task:** zero deliberate exchange create/modify/cancel operations. The migration and deployment were Production writes; no Real trade/order was manufactured or modified by the verification operator. DB recorded zero new Real entry attempts post-release. We did **not** perform a complete private exchange-wide order-history audit and do not label that as proven zero.

## Anomalies and limitations

- **Pre-existing log destination failure, active safety blocker:** `logToGitHub` returned HTTP 403 because the issue exceeded its 2500-comment limit. There were 317 such lines between 15:00 and 16:11 UTC **before** this release, and **44** between release completion at 16:12:22 and the final 16:22:20 UTC check. The rates are similar, so the fault is not attributed to c518, but it nevertheless fails the no-repeated-API-error pre-live-write gate. Three additional initial `failed` keyword matches were successful stablecoin collector counters (`failed=0`); PostgREST had zero error/schema lines in the immediate observed window. Repair the logging destination and recheck before any operator-initiated exchange write; do not infer native protection from app health.
- **Legacy MEXC data mismatch:** four old disconnected-account `OPEN` rows have no protection IDs/sync evidence. This is an investigation item; neither safe exchange protection nor data corruption was inferred from database labels alone. Do not act on these rows without owner/account reconciliation.
- **Global TypeScript check:** exact-candidate offline audit had 52→52 existing diagnostics, zero new; it was not globally green. Offline exact-candidate focused Futures 294/294, ancillary 153/153, Spot commission 64/64, Spot clamp 25/25, web/admin builds and two API bundles passed before authorization. CI additionally passed its full configured workflow. No unsafe legacy credential-loading test suite was run.
- **Shadow:** the independent 24-hour window ends 2026-09-23 07:30 UTC and must be assessed under its separate frozen protocol. An active unit now is not a result or profitability proof.
- **No rollback** was performed. Do not down-migrate Production or clear any durable uncertain entry hold to make a status appear green.

## Files inspected / changed and publication state

Inspected: `.github/workflows/production-ci.yml`, `ops/signalverse-deploy`, the two specified migration files, `api/copytrade.ts`, `api/analyze.ts`, the safe test runbook, current handoffs, backup script and VPS deploy receiver metadata. Production application source **was not edited after c518**. Production DB changed only by the two authorized Futures migrations and schema-cache notification. Application Git `main` documentation merge `e9aced1...` added `docs/testing/real-futures-production-release-c518-2026-09-22.md`, preserved two prior Spot reports and updated `HANDOFF.md`; it did not activate a second release. This AI-Log file is the sanitized public audit record; no credentials, account IDs, raw trades or dumps are included.

## Final status and next step

**RELEASE BLOCKED — repeated GitHub logging API 403 fails the pre-live-write safety gate.** Live Production SHA is exactly `c518c3e66a68afc5c7e8e45d8ed32d198364419a`, distinct from documentation merge `e9aced1...`. Backup/migration/schema/CI/deploy/core-runtime and bounded mode-health gates passed; this is **not a failed deploy or a rollback**. Operator-initiated native protection writes must remain stopped until the log destination is repaired and the repeated-error gate rechecked. Naturally eligible connected open Binance/MEXC positions and a naturally closed post-release Real trade are also absent; never manufacture them. Separately reconcile the old disconnected MEXC rows. No profitability claim is warranted.
